import importlib,importlib.metadata as md,json,socket,sys;R={'pandas':'pandas','xlrd':'xlrd','openpyxl':'openpyxl'};D={n:{'available':True,'version':md.version(d),'origin':getattr(importlib.import_module(n),'__file__',None)} for n,d in R.items()};S=__import__('pyspark.sql',fromlist=['SparkSession']).SparkSession;s=S.getActiveSession() or S.builder.getOrCreate();C='connect' in str(type(s)).lower() or 'connect' in type(s).__module__.lower();o={'required_constraints':['pandas>=2.2,<3','xlrd>=2.0.1','openpyxl>=3.1.0'],'driver':D,'executor':[],'spark_available':True,'spark_version':s.version,'executor_status':'SKIPPED_SPARK_CONNECT' if C else 'NOT_CHECKED','driver_ok':all(x['available'] for x in D.values()),'executor_ok':True};o['status']='PASS' if o['driver_ok'] and o['executor_ok'] else 'FAIL';print(json.dumps(o,ensure_ascii=False,indent=2,default=str));assert o['status']=='PASS','Validação reprovada: não execute as ingestões Bronze.'
